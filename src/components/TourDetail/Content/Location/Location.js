import classNames from 'classnames/bind';
import styles from './Location.module.scss';

const cx = classNames.bind(styles);

function Location() {
    return <h3>This is Location !!!</h3>;
}

export default Location;
