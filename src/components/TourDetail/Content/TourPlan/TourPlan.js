import classNames from 'classnames/bind';
import styles from './TourPlan.module.scss';

const cx = classNames.bind(styles);

function TourPlan() {
    return <h3>This is TourPlan !!!</h3>;
}

export default TourPlan;
